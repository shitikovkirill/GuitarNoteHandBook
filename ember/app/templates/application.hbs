<nav class="navbar navbar-full navbar-dark bg-primary">
  <div class="container">
    {{#link-to 'application' class="navbar navbar-brand"}}Home{{/link-to}}
    <div class="pull-sm-right">
      {{#if session.isAuthenticated}}
        <button {{action 'logout'}} class="btn btn-secondary-outline">Logout</button>
      {{else}}
        {{#link-to 'login' class="btn btn-secondary-outline"}}
          Login
        {{/link-to}}
      {{/if}}
    </div>
  </div>
</nav>

<div class="container main-container">
  <div class="row">
    <nav class="col-sm-2">
      <ul class="nav nav-stacked nav-pills">
        <li class="nav-item">
          {{#link-to 'author' class="nav-link"}}Авторы{{/link-to}}
        </li>
        <li class="nav-item">
          {{#link-to 'composition' class="nav-link"}}Произведения{{/link-to}}
        </li>
      </ul>
    </nav>
    <div class="col-sm-10">
      {{outlet}}
    </div>
  </div>
</div>

